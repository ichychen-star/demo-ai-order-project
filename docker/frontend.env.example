# Frontend environment variables
# Copy to frontend.env and fill in real values — never commit frontend.env

# When running via Docker Compose, backend is accessible on port 8080 from the host
# (not from within the frontend container — use the Nginx proxy in production)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
